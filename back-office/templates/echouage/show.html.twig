{% extends 'layout.html.twig' %}

{% block title %}Echouage{% endblock %}

{% block main %}
    <h1>Echouage</h1>

    <a class="show" href="{{ path('echouage_index') }}">back to list</a>

    <a class="show" href="{{ path('echouage_edit', {'id': echouage.id}) }}">edit</a>

    {{ include('echouage/_delete_form.html.twig') }}

    <table class="table">
        <tmain>
            <tr>
                <th>Id</th>
                <td>{{ echouage.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ echouage.date }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ echouage.nombre }}</td>
            </tr>
        </tmain>
    </table>
{% endblock %}
