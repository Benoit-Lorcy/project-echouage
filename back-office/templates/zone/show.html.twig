{% extends "layout.html.twig" %}

{% block title %}Zone{% endblock %}

{% block main %}
    <h1>Zone</h1>

    <a class="show" href="{{ path('zone_index') }}">back to list</a>

    <a class="show" href="{{ path('zone_edit', {'id': zone.id}) }}">edit</a>

    {{ include('zone/_delete_form.html.twig') }}

    <table class="table">
        <tmain>
            <tr>
                <th>Id</th>
                <td>{{ zone.id }}</td>
            </tr>
            <tr>
                <th>Zone</th>
                <td>{{ zone.zone }}</td>
            </tr>
        </tmain>
    </table>
{% endblock %}
